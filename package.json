{
  "name": "secret_msg",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@secret-hub/eslint-config": "*",
    "@secret-hub/typescript-config": "*",
    "@types/bcryptjs": "^2.4.6",
    "husky": "^9.0.11",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@3.2.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "react-email": "^2.1.1",
    "resend": "^3.2.0",
    "zod": "^3.22.4"
  }
}
